export enum Server2ClientEvents {
  GENERATING_STATUS = "generating_status"
}

export enum Client2ServerEvents {
  _DISCONNECT = "disconnect",
  DISCONNECT = "g_disconnect",
  GENERATE = "generate"
}
